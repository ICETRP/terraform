trigger:
  - main

pool:
  vmImage: 'ubuntu-latest'

variables:
  storageAccountName: 'tfstate73829'   # ← YOUR REAL STORAGE ACCOUNT NAME

steps:
# Install Terraform
- bash: |
    wget -O- https://apt.releases.hashicorp.com/gpg | gpg --dearmor | sudo tee /usr/share/keyrings/hashicorp-archive-keyring.gpg >/dev/null
    echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
    sudo apt update && sudo apt install terraform -y
  displayName: 'Install Terraform'

# Terraform Init – now ARM_* variables are injected automatically
- bash: |
    terraform init \
      -backend-config="resource_group_name=tf-win-rg" \
      -backend-config="storage_account_name=$(storageAccountName)" \
      -backend-config="container_name=tfstate" \
      -backend-config="key=azure-vm.tfstate"
  displayName: 'Terraform Init'

# Plan + Apply
- bash: terraform plan -out=tfplan
  displayName: 'Terraform Plan'

- bash: terraform apply -auto-approve tfplan
  displayName: 'Terraform Apply – Creates your Ubuntu VM'

# Show IP
- bash: |
    echo "SUCCESS! YOUR VM IS READY"
    echo "Public IP: $(terraform output -raw public_ip)"
    echo "SSH: ssh -i azure-key.pem azureuser@$(terraform output -raw public_ip)"
  displayName: 'Show Public IP & SSH Command'