trigger:
  - main

pool:
  vmImage: 'ubuntu-latest'

steps:
# Install Terraform
- bash: |
    wget -O- https://apt.releases.hashicorp.com/gpg | gpg --dearmor | sudo tee /usr/share/keyrings/hashicorp-archive-keyring.gpg >/dev/null
    echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
    sudo apt update && sudo apt install terraform -y
  displayName: 'Install Terraform'

# OIDC login using the NEW federated connection
- task: AzureCLI@2
  inputs:
    azureSubscription: 'azure-oidc'   # the new one you just created
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript: echo "OIDC login successful"

# Run Terraform — local state, no backend, no auth errors
- bash: |
    terraform init -backend=false -reconfigure
    terraform plan -out=tfplan
    terraform apply -auto-approve tfplan
  displayName: 'Create Ubuntu VM'

# Show IP
- bash: |
    echo ""
    echo "VM CREATED!"
    echo "Public IP   : $(terraform output -raw public_ip)"
    echo "SSH command : ssh -i azure-key.pem azureuser@$(terraform output -raw public_ip)"
    echo ""
  displayName: 'SHOW IP & SSH'